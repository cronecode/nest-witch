html
  head
   meta(charset='utf-8')
   script(src="https://code.jquery.com/jquery-3.0.0.min.js" integrity="sha256-JmvOoLtYsmqlsWxa7mDSLMwa6dZ9rrIdtrrVYRnDRH0=" crossorigin="anonymous")

  body
    div(id='profile')
      div(id='first')

    h1 THIS IS A PROFILE
    button(id='reset') RESET
    button(id='refine') REFINE

    script.

      $('#reset').click(function(){
        window.localStorage.clear()
        location.href = location.origin + '/'
      })

      $('#refine').click(function(){
        var player = JSON.parse(window.localStorage.getItem('player'))
        console.log(player)
        var pattern = player.pattern
        console.log(pattern)
        $.post('/home', {pattern: pattern})
          .done(function(data){
            player.episode = data
            player.index = 0
            player.choice = {}
            console.log(player)
            window.localStorage.setItem('player', JSON.stringify(player))
            location.href = location.origin + '/game'
          })
      })
